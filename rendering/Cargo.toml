[package]
name = "rendering"
version = "0.1.0"
authors = ["doomguy"]
edition = "2021"
build = "build.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
sys = { path = "../sys" }

[build-dependencies]
gl_generator = "*"
